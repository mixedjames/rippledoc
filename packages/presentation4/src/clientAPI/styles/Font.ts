/**
 * CSS numeric font-weight values.
 *
 * Named aliases (normal = 400, bold = 700) are intentionally excluded — authors
 * use numbers so the type is unambiguous and the view needs no mapping table.
 */
// eslint-disable-next-line no-magic-numbers
export type FontWeight = 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900;
