/**
 * Runtime transform state for a Section.
 *
 * This is intentionally minimal for now. It will later
 * grow to include properties like parallax offsets,
 * background transforms, etc.
 */
export class SectionTransform {}
