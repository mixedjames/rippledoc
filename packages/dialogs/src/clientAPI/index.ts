/**
 * clientAPI — the public interface set for dialogs.
 *
 * Everything here is a pure interface or type. Concrete implementations
 * are never exported; use the factory function as the sole construction point.
 */
